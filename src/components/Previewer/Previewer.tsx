import { Card } from "../ui/card";

export const Previewer = () => {
  return (
    <div className="pl-4">
      <Card className="fixed w-[31%] z-0 mt-20 h-[85%]">
        Space to preview friends list, event details, event planning form, etc.
      </Card>
    </div>
  );
};
